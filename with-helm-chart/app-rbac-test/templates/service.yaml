apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-rbac-test.fullname" . }}
  labels:
    {{- include "app-rbac-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-rbac-test.selectorLabels" . | nindent 4 }}
